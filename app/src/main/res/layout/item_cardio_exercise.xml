<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardBackgroundColor="#111111"
    app:cardCornerRadius="16dp"
    app:strokeColor="#1E1E1E"
    app:strokeWidth="1dp"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- ── Row 1: name + done check ── -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvCardioExerciseName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Running"
                    android:textColor="#F9FAFB"
                    android:textSize="16sp"
                    android:textStyle="bold"/>

                <TextView
                    android:id="@+id/tvCardioDuration"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="30 min planned"
                    android:textColor="#6B7280"
                    android:textSize="13sp"
                    android:layout_marginTop="4dp"/>

            </LinearLayout>

            <!-- Done checkmark — shown when card tapped -->
            <TextView
                android:id="@+id/tvCardioDoneCheck"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="✓"
                android:textColor="#22C55E"
                android:textSize="20sp"
                android:textStyle="bold"
                android:visibility="gone"/>

        </LinearLayout>

        <!-- ── Divider ── -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#1E1E1E"
            android:layout_marginBottom="14dp"/>

        <!-- ── Row 2: chrono display ── -->
        <TextView
            android:id="@+id/tvCardioChronoTime"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="00:00"
            android:textColor="#FF6D00"
            android:textSize="36sp"
            android:textStyle="bold"
            android:gravity="center"
            android:fontFamily="monospace"
            android:layout_marginBottom="14dp"/>

        <!-- ── Row 3: ▶ / ⏸  and  Reset buttons ── -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnCardioStartTimer"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:text="▶"
                android:textSize="18sp"
                android:textColor="#FFFFFF"
                android:layout_marginEnd="8dp"
                app:backgroundTint="#22C55E"
                app:cornerRadius="12dp"
                app:strokeWidth="0dp"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnCardioResetTimer"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:text="↺ Reset"
                android:textSize="14sp"
                android:textColor="#9CA3AF"
                app:backgroundTint="#1A1A1A"
                app:cornerRadius="12dp"
                app:strokeColor="#2A2A2A"
                app:strokeWidth="1dp"/>

        </LinearLayout>

        <!-- ── Hint: tap card to mark done ── -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Tap card to mark done"
            android:textColor="#374151"
            android:textSize="11sp"
            android:gravity="center"
            android:layout_marginTop="12dp"/>

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>